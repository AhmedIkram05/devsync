apiVersion: v1
kind: Service
metadata:
  name: devsync-frontend
  namespace: devsync
  labels:
    app.kubernetes.io/part-of: devsync
spec:
  type: ClusterIP
  selector:
    app: devsync-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
