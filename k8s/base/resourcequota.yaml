apiVersion: v1
kind: ResourceQuota
metadata:
  name: devsync-quota
  namespace: devsync
  labels:
    app.kubernetes.io/part-of: devsync
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    pods: "20"
